version: 2

models:
  - name: bdg_sales_order_detail_reason
    description: Table linking customers, sales orders, and associated reasons for sales.
    columns:
      - name: sales_order_fk
        description: Foreign key referencing the sales order.

      - name: customer_fk
        description: Foreign key referencing the customer.

      - name: ship_address_fk
        description: Foreign key referencing the shipping address.

      - name: address_fk
        description: Foreign key referencing the customer address.

      - name: entity_address_fk
        description: Foreign key referencing the customer to the address.

      - name: sales_order_detail_fk
        description: Foreign key referencing the sales order detail.

      - name: product_fk
        description: Foreign key referencing the product.

      - name: sales_reason_fk
        description: Foreign key referencing the sales reason.
